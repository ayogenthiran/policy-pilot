# Ruff — install deps from requirements.txt; optional: pip install ruff
[tool.ruff]
target-version = "py310"
line-length = 100
src = ["policy_pilot", "scripts", "."]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.format]
quote-style = "double"
